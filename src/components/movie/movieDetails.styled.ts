import styled from "styled-components"

export const Details = styled.div`
  line-height: 1.4;
  font-size: 1.4rem;

  header {
    display: flex;
  }

  section {
    padding: 4rem;
    display: flex;
    flex-direction: column;
    gap: 1.6rem;
  }

  img {
    width: 33%;
    height: auto;
  }
`

